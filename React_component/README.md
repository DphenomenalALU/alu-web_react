# React component project

This project converts the React Props dashboard into class components and adds
component lifecycles, reusable sections, higher-order components, and render
performance optimizations.

Each numbered task is kept in its own directory so the progression can be
reviewed independently.
