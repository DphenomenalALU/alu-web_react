# Webpack

Webpack exercises for the ALU Webpack project.

Each task is self-contained and can be installed and built from its own
directory:

```sh
cd task_0
npm install
npm run build
```

Tasks 1 and 2 use production builds. Task 3 also provides a development
server at `http://localhost:8564/`.
