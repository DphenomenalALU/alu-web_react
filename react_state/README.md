# React state

This project extends the React dashboard with local component state, controlled
forms, React Context, advanced state updates, and a React Hooks interaction.

Each numbered task is a standalone snapshot of the dashboard as the state
features are introduced.
