# React inline styling project

This project migrates the React dashboard from CSS files to Aphrodite styles,
adds responsive behavior, and animates the notifications menu.

Each numbered task contains a reviewable snapshot of the implementation.
